route:
  receiver: platform-operators
  group_by: [alertname, instance]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
receivers:
  - name: platform-operators
    webhook_configs:
      - url_file: /run/secrets/alert_webhook
        send_resolved: true
