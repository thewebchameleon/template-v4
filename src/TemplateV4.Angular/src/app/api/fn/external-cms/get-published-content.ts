/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PublishedContent } from '../../models/published-content';

export interface GetPublishedContent$Params {
  key: string;
  id: string;
  fields?: string;
  expand?: number;
}

export function getPublishedContent(http: HttpClient, rootUrl: string, params: GetPublishedContent$Params, context?: HttpContext): Observable<StrictHttpResponse<PublishedContent>> {
  const rb = new RequestBuilder(rootUrl, getPublishedContent.PATH, 'get');
  if (params) {
    rb.path('key', params.key, {});
    rb.path('id', params.id, {});
    rb.query('fields', params.fields, {});
    rb.query('expand', params.expand, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PublishedContent>;
    })
  );
}

getPublishedContent.PATH = '/api/external/cms/collections/{key}/items/{id}';
