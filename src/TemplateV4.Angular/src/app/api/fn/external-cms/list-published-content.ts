/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfPublishedContent } from '../../models/page-of-published-content';

export interface ListPublishedContent$Params {
  key: string;
  PageNumber?: number;
  PageSize?: number;
  Search?: string;
  Sort?: string;
  Direction?: string;
  State?: string;
  Filter?: string;
  fields?: string;
  expand?: number;
}

export function listPublishedContent(http: HttpClient, rootUrl: string, params: ListPublishedContent$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfPublishedContent>> {
  const rb = new RequestBuilder(rootUrl, listPublishedContent.PATH, 'get');
  if (params) {
    rb.path('key', params.key, {});
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('Search', params.Search, {});
    rb.query('Sort', params.Sort, {});
    rb.query('Direction', params.Direction, {});
    rb.query('State', params.State, {});
    rb.query('Filter', params.Filter, {});
    rb.query('fields', params.fields, {});
    rb.query('expand', params.expand, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfPublishedContent>;
    })
  );
}

listPublishedContent.PATH = '/api/external/cms/collections/{key}/items';
