/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfBlogSummary } from '../../models/page-of-blog-summary';

export interface ListExternalCmsArticles$Params {
  pageNumber?: number;
  pageSize?: number;
}

export function listExternalCmsArticles(http: HttpClient, rootUrl: string, params?: ListExternalCmsArticles$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfBlogSummary>> {
  const rb = new RequestBuilder(rootUrl, listExternalCmsArticles.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfBlogSummary>;
    })
  );
}

listExternalCmsArticles.PATH = '/api/external/cms/articles';
