/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfActionItemDto } from '../../models/page-of-action-item-dto';

export interface ListActionItems$Params {
  PageNumber?: number;
  PageSize?: number;
  Scope?: string;
  State?: string;
  Sort?: string;
  Direction?: string;
  Search?: string;
}

export function listActionItems(http: HttpClient, rootUrl: string, params?: ListActionItems$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfActionItemDto>> {
  const rb = new RequestBuilder(rootUrl, listActionItems.PATH, 'get');
  if (params) {
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('Scope', params.Scope, {});
    rb.query('State', params.State, {});
    rb.query('Sort', params.Sort, {});
    rb.query('Direction', params.Direction, {});
    rb.query('Search', params.Search, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfActionItemDto>;
    })
  );
}

listActionItems.PATH = '/api/v1/auth/action-items';
