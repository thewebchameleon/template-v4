/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PrivateModuleRegistrationStatus } from '../../models/private-module-registration-status';

export interface GetPrivateModuleRegistration$Params {
}

export function getPrivateModuleRegistration(http: HttpClient, rootUrl: string, params?: GetPrivateModuleRegistration$Params, context?: HttpContext): Observable<StrictHttpResponse<PrivateModuleRegistrationStatus>> {
  const rb = new RequestBuilder(rootUrl, getPrivateModuleRegistration.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PrivateModuleRegistrationStatus>;
    })
  );
}

getPrivateModuleRegistration.PATH = '/api/v1/auth/private-modules';
