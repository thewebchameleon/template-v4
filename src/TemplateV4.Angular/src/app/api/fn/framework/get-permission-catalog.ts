/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfPermissionItem } from '../../models/page-of-permission-item';

export interface GetPermissionCatalog$Params {
  pageNumber?: number;
  pageSize?: number;
  search?: string;
  matchingKeys?: string;
  sort?: string;
  direction?: string;
}

export function getPermissionCatalog(http: HttpClient, rootUrl: string, params?: GetPermissionCatalog$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfPermissionItem>> {
  const rb = new RequestBuilder(rootUrl, getPermissionCatalog.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('search', params.search, {});
    rb.query('matchingKeys', params.matchingKeys, {});
    rb.query('sort', params.sort, {});
    rb.query('direction', params.direction, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfPermissionItem>;
    })
  );
}

getPermissionCatalog.PATH = '/api/v1/roles/permissions';
