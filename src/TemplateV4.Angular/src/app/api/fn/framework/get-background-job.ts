/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BackgroundJobDetail } from '../../models/background-job-detail';

export interface GetBackgroundJob$Params {
  id: string;
}

export function getBackgroundJob(http: HttpClient, rootUrl: string, params: GetBackgroundJob$Params, context?: HttpContext): Observable<StrictHttpResponse<BackgroundJobDetail>> {
  const rb = new RequestBuilder(rootUrl, getBackgroundJob.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BackgroundJobDetail>;
    })
  );
}

getBackgroundJob.PATH = '/api/v1/auth/administration/background-jobs/{id}';
