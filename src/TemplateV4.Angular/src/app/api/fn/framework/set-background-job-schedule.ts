/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BackgroundJobPauseRequest } from '../../models/background-job-pause-request';
import { BackgroundJobSummary } from '../../models/background-job-summary';

export interface SetBackgroundJobSchedule$Params {
  id: string;

/**
 * Anonymous-bound antiforgery token returned by GET /api/v1/auth/csrf. The browser must also send an exact allowed Origin.
 */
  'X-CSRF-TOKEN': string;
      body: BackgroundJobPauseRequest
}

export function setBackgroundJobSchedule(http: HttpClient, rootUrl: string, params: SetBackgroundJobSchedule$Params, context?: HttpContext): Observable<StrictHttpResponse<BackgroundJobSummary>> {
  const rb = new RequestBuilder(rootUrl, setBackgroundJobSchedule.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-CSRF-TOKEN', params['X-CSRF-TOKEN'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BackgroundJobSummary>;
    })
  );
}

setBackgroundJobSchedule.PATH = '/api/v1/auth/administration/background-jobs/{id}/schedule';
