/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FileShareItem } from '../../models/file-share-item';
import { FileShareRequest } from '../../models/file-share-request';

export interface ShareFileStorageFile$Params {
  id: string;

/**
 * Anonymous-bound antiforgery token returned by GET /api/v1/auth/csrf. The browser must also send an exact allowed Origin.
 */
  'X-CSRF-TOKEN': string;
      body: FileShareRequest
}

export function shareFileStorageFile(http: HttpClient, rootUrl: string, params: ShareFileStorageFile$Params, context?: HttpContext): Observable<StrictHttpResponse<FileShareItem>> {
  const rb = new RequestBuilder(rootUrl, shareFileStorageFile.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-CSRF-TOKEN', params['X-CSRF-TOKEN'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FileShareItem>;
    })
  );
}

shareFileStorageFile.PATH = '/api/v1/auth/file-storage/{id}/shares';
