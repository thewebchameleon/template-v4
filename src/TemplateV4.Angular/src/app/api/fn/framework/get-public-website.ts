/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PublicWebsite } from '../../models/public-website';

export interface GetPublicWebsite$Params {
}

export function getPublicWebsite(http: HttpClient, rootUrl: string, params?: GetPublicWebsite$Params, context?: HttpContext): Observable<StrictHttpResponse<PublicWebsite>> {
  const rb = new RequestBuilder(rootUrl, getPublicWebsite.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PublicWebsite>;
    })
  );
}

getPublicWebsite.PATH = '/api/v1/website';
