/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ContentItem } from '../../models/content-item';

export interface GetContentItem$Params {
  key: string;
  id: string;
}

export function getContentItem(http: HttpClient, rootUrl: string, params: GetContentItem$Params, context?: HttpContext): Observable<StrictHttpResponse<ContentItem>> {
  const rb = new RequestBuilder(rootUrl, getContentItem.PATH, 'get');
  if (params) {
    rb.path('key', params.key, {});
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ContentItem>;
    })
  );
}

getContentItem.PATH = '/api/v1/auth/cms/collections/{key}/items/{id}';
