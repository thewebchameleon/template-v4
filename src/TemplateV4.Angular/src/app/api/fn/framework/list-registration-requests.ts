/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfRegistrationReviewItem } from '../../models/page-of-registration-review-item';

export interface ListRegistrationRequests$Params {
  pageNumber?: number;
  pageSize?: number;
  sort?: string;
  direction?: string;
}

export function listRegistrationRequests(http: HttpClient, rootUrl: string, params?: ListRegistrationRequests$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfRegistrationReviewItem>> {
  const rb = new RequestBuilder(rootUrl, listRegistrationRequests.PATH, 'get');
  if (params) {
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('sort', params.sort, {});
    rb.query('direction', params.direction, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfRegistrationReviewItem>;
    })
  );
}

listRegistrationRequests.PATH = '/api/v1/auth/registration-requests';
