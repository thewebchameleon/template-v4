/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface DownloadPublicMyFile$Params {
  id: string;
  'X-File-Share'?: string;
}

export function downloadPublicMyFile(http: HttpClient, rootUrl: string, params: DownloadPublicMyFile$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, downloadPublicMyFile.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-File-Share', params['X-File-Share'], {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

downloadPublicMyFile.PATH = '/api/v1/auth/my-files/public/{id}/download';
