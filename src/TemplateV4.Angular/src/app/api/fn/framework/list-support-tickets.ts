/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfTicketItem } from '../../models/page-of-ticket-item';

export interface ListSupportTickets$Params {
  PageNumber?: number;
  PageSize?: number;
  Search?: string;
  Status?: string;
  Priority?: string;
  Category?: string;
  Assignee?: string;
  Queue?: boolean;
  Sort?: string;
  Direction?: string;
}

export function listSupportTickets(http: HttpClient, rootUrl: string, params?: ListSupportTickets$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfTicketItem>> {
  const rb = new RequestBuilder(rootUrl, listSupportTickets.PATH, 'get');
  if (params) {
    rb.query('PageNumber', params.PageNumber, {});
    rb.query('PageSize', params.PageSize, {});
    rb.query('Search', params.Search, {});
    rb.query('Status', params.Status, {});
    rb.query('Priority', params.Priority, {});
    rb.query('Category', params.Category, {});
    rb.query('Assignee', params.Assignee, {});
    rb.query('Queue', params.Queue, {});
    rb.query('Sort', params.Sort, {});
    rb.query('Direction', params.Direction, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfTicketItem>;
    })
  );
}

listSupportTickets.PATH = '/api/v1/auth/support';
