/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BackgroundJobPage } from '../../models/background-job-page';

export interface ListBackgroundJobs$Params {
  search?: string;
  status?: string;
  pageNumber?: number;
  pageSize?: number;
  sort?: string;
  direction?: string;
}

export function listBackgroundJobs(http: HttpClient, rootUrl: string, params?: ListBackgroundJobs$Params, context?: HttpContext): Observable<StrictHttpResponse<BackgroundJobPage>> {
  const rb = new RequestBuilder(rootUrl, listBackgroundJobs.PATH, 'get');
  if (params) {
    rb.query('search', params.search, {});
    rb.query('status', params.status, {});
    rb.query('pageNumber', params.pageNumber, {});
    rb.query('pageSize', params.pageSize, {});
    rb.query('sort', params.sort, {});
    rb.query('direction', params.direction, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BackgroundJobPage>;
    })
  );
}

listBackgroundJobs.PATH = '/api/v1/auth/administration/background-jobs';
