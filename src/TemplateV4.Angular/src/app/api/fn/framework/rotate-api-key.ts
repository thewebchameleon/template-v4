/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiKeyCreated } from '../../models/api-key-created';

export interface RotateApiKey$Params {
  id: string;

/**
 * Anonymous-bound antiforgery token returned by GET /api/v1/auth/csrf. The browser must also send an exact allowed Origin.
 */
  'X-CSRF-TOKEN': string;
}

export function rotateApiKey(http: HttpClient, rootUrl: string, params: RotateApiKey$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiKeyCreated>> {
  const rb = new RequestBuilder(rootUrl, rotateApiKey.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.header('X-CSRF-TOKEN', params['X-CSRF-TOKEN'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiKeyCreated>;
    })
  );
}

rotateApiKey.PATH = '/api/v1/auth/administration/api-keys/{id}/rotate';
