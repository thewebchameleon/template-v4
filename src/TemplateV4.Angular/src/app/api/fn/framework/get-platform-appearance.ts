/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PlatformAppearance } from '../../models/platform-appearance';

export interface GetPlatformAppearance$Params {
}

export function getPlatformAppearance(http: HttpClient, rootUrl: string, params?: GetPlatformAppearance$Params, context?: HttpContext): Observable<StrictHttpResponse<PlatformAppearance>> {
  const rb = new RequestBuilder(rootUrl, getPlatformAppearance.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PlatformAppearance>;
    })
  );
}

getPlatformAppearance.PATH = '/api/v1/auth/configuration/appearance';
